version: '3'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes: 
      - .:/lab01
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    depends_on:
      - db
  db:
    image: postgres
    environment: 
      - POSTGRES_PASSWORD=gnoul256
      - POSTGRES_USER=luongbc
